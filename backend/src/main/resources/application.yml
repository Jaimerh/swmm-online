spring:
  application:
    name: swmm-online
  profiles:
    active: dev
server:
  port: 8080
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.example.swmm.entity
  configuration:
    map-underscore-to-camel-case: true
